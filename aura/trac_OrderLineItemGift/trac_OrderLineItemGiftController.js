/**
 * Created by macuser on 8/12/20.
 */

({
    init : function(component, event, helper) {
        helper.setGiftOptions(component, event, helper)
        helper.setIsBM(component, event, helper)
    }
});